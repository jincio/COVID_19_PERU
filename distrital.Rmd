---
title: "Distrital"
---
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE)
```

```{r library}
library(tidyverse)
library(sf)

info_distrital <- readxl::read_xlsx("data/informacion_distritos.xlsx")
mapa_distrital <- read_sf("data/distrital_shp/Distritos.shp")
```

```{r eda}
info_distrital %>% 
  group_by(Fecha) %>% 
  tally()
# se encuentra que hay una observacion fechada como 2020-07-04, se asume que debería ser 2020-04-07
```

```{r cumsum}
info_distrital %>% 
  mutate(Fecha = lubridate::as_date(Fecha)) %>% 
  filter(Fecha == "2020-04-26") %>% 
  group_by(Region) %>% 
  tally()
```



